#-----------------------------------------------------------------------------
#@TYPE: Machine
#@NAME: Google Pixel 7 (codename panther)
#@DESCRIPTION: Machine configuration for the Google Pixel 7, Tensor G2 gs201
#
# This machine exists to build the *boot image*, the way halium-arm64.conf says
# a device machine should: the rootfs comes from halium-arm64 and the GSI is
# chosen at install time. Two things come out of here:
#
#   MACHINE=panther bitbake initramfs-android-image   the Halium initramfs
#   MACHINE=panther bitbake luneos-bootimg-gki        boot-panther-luneos.img,
#                                                     init_boot-panther-luneos.img
#                                                     (+ the -debug variant)
#
# Tier A: the kernel is the Android Common Kernel android14-6.1 built by
# Google's Kleaf tooling with the clang the KMI was frozen with, plus the
# LuneOS defconfig fragment - see the luneos-bootimg-bluejay repository (the
# same GKI kernel: the stock boot.img kernel is byte-identical on both devices). It is
# not, and cannot be, built by this tree: an OE-toolchain rebuild would move
# the exported symbol CRCs under CONFIG_MODVERSIONS and the phone's own stock
# vendor modules would stop loading. Point GKI_KERNEL_IMAGE at the Image it
# produced.
#-----------------------------------------------------------------------------

require conf/machine/include/arm/arch-arm64.inc
require conf/machine/include/meta-android-halium.inc

# No kernel is built here (see above), but virtual/kernel still has to resolve.
PREFERRED_PROVIDER_virtual/kernel = "linux-dummy"
MACHINE_ESSENTIAL_EXTRA_RDEPENDS = ""
MACHINE_ESSENTIAL_EXTRA_RRECOMMENDS = ""

MACHINE_FEATURES = "apm alsa bluetooth gps usbgadget usbhost phone wifi vfat ext2 keyboard nfc"

# 6.3" 1080x2400. Google publishes 416 ppi; the arithmetic on the rounded
# diagonal gives 418. UNVERIFIED on hardware - sargo needed a device pixel
# ratio well away from the natural one.
MACHINE_DISPLAY_WIDTH_PIXELS = "1080"
MACHINE_DISPLAY_HEIGHT_PIXELS = "2400"
MACHINE_DISPLAY_ORIENTATION = "0"
MACHINE_DISPLAY_PPI = "416"

# Tracks the GSI generation, not the Android 16 vendor: libhybris gates its
# per-generation support on ANDROID_VERSION_MAJOR, which comes from this
# package. Same reasoning as sargo.conf, which spells it out at length.
PREFERRED_VERSION_android-headers-halium = "16.0%"

# Treble-era GSI, so the modern droid modules rather than the jb2q ones the
# halium include defaults to.
PREFERRED_VERSION_pulseaudio-modules-droid = "17.0.103%"

# No GSI baked in, as on halium-arm64: rootfs.img and android-rootfs.img sit
# side by side in userdata and the GSI is picked at install time from the
# device's vendor API level. The flash kit ships the Halium 16 one.
VIRTUAL-RUNTIME_android-system-image = ""

# Deliberately no XSERVER: this machine builds no rootfs, and the stack here is
# hwcomposer-based, which xf86-video-fbdev does not serve.

IMAGE_FSTYPES += "tar.gz"
IMAGE_ROOTFS_EXTRA_SPACE = "200000"

# Boot image: header v4, and panther is an Android-13-launch device, so the
# kernel is alone in boot and the generic ramdisk has its own init_boot
# partition - only 0x800000 (8 MB), against 64 MB for boot. Verified against
# the stock bp4a images (boot.img: kernel 16,565,921 B, ramdisk size 0;
# init_boot.img: kernel 0, ramdisk 2,657,135 B) and against
# device/google/gs201/BoardConfig-common.mk. The kernel modules are in yet
# another partition here, vendor_kernel_boot; it and vendor_boot stay stock and
# the bootloader merges them underneath our ramdisk, so /lib/modules is there
# for the initramfs module loader.
#
# v4 has no page size, dtb, second or tags field: the device tree is in
# vendor_kernel_boot and the load addresses are the bootloader's business.
ANDROID_BOOTIMG_HEADER_VERSION = "4"
ANDROID_BOOTIMG_INIT_BOOT = "1"

# Empty on purpose. The kernel command line comes from vendor_boot, which stays
# stock, and a Pixel bootloader takes only the arguments it already knows from
# the boot image - verified on sargo, where even a canary never reached
# /proc/cmdline. Anything LuneOS needs goes into CONFIG_CMDLINE instead; see
# halium-kernel.inc.
ANDROID_BOOTIMG_CMDLINE = ""

# From the stock vendor_boot command line: console=ttySAC0,115200.
# UNVERIFIED on hardware - a retail Pixel exposes no debug UART.
SERIAL_CONSOLE = "115200 ttySAC0"
